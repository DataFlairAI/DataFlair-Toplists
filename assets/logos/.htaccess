# Allow direct access to logo images
<FilesMatch "\.(jpg|jpeg|png|gif|webp|svg)$">
    Order Allow,Deny
    Allow from all
</FilesMatch>

# Deny access to PHP files for security
<FilesMatch "\.php$">
    Order Deny,Allow
    Deny from all
</FilesMatch>
